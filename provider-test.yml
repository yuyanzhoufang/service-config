server:
  port: 4445
spring:
  application:
    name: provider
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka
  instance:
    instance-id: provider
    prefer-ip-address: true
